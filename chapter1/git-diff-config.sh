git config --global color.diff.meta
git config --global color.diff.frag
git config --global color.diff.commit
git config --global color.diff.old
git config --global color.diff.new
git config --global color.diff.whitespace "red reverse"

## 참고로 저자는 다음과 같은 diff-highlight 색상을 선호한다. 
## 그 이유는 저자는 Solarized light 테마를 사용하고 있는데, 다음 색상의 수정 사항들이 눈에 더 잘 띄 기 때문이다.
#git config --global color.diff-highlight.oldNormal "red"
#git config --global color.diff-highlight.oldHighlight "red 217" git config --global color.diff-highlight.newNormal "green"
#git config --global color.diff-highlight.newHighlight "green 157"
   
